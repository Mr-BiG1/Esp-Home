# Production Database Connection String (PostgreSQL)
# DATABASE_URL="postgresql://postgres:password@localhost:5432/smarthome?schema=public"

# SQLite local fallback (for standalone dev without external DB)
DATABASE_URL="file:./dev.db"

# JWT Secret for Web Dashboard Auth
JWT_SECRET="dev_jwt_secret_change_me_in_production_981247"

# Pairing Token Master Secret
PAIRING_SECRET="pair_master_secret_2026_x89"

# System Environment
NODE_ENV="development"
